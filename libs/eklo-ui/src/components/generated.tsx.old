import React from "react";
import styled, { ThemeProvider, createGlobalStyle } from "styled-components";
import Image from "next/image";

// Theme
const theme = {
  colors: {
    red: "#FE5442",
    pink: "#FCE4D8",
    yellow: "#FDF6BF",
    green: "#007238",
    lightGreen: "#732E78",
    purple: "#99D1CE",
    blue: "#DAD4EF",
  },
  fonts: {
    heading: '"Dela Gothic One", cursive',
    body: '"Gotham Medium", sans-serif',
  },
  fontSizes: {
    paragraph: "24px",
    subHeading: "24px",
    heading: {
      small: "40px",
      medium: "56px",
      large: "56px",
    },
  },
  breakpoints: {
    tablet: "768px",
    desktop: "1024px",
  },
};

// Global styles
const GlobalStyle = createGlobalStyle`
  @import url('https://fonts.googleapis.com/css2?family=Dela+Gothic+One&display=swap');
  
  body {
    font-family: ${(props) => props.theme.fonts.body};
    background-color: ${(props) => props.theme.colors.pink};
    color: ${(props) => props.theme.colors.green};
    margin: 0;
    padding: 0;
    font-size: ${(props) => props.theme.fontSizes.paragraph};
  }
`;

// Styled components
const Header = styled.header`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem;
  background-color: ${(props) => props.theme.colors.red};

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    padding: 1rem 2rem;
  }
`;

const Logo = styled.h1`
  font-family: ${(props) => props.theme.fonts.heading};
  color: white;
  margin: 0;
  font-size: ${(props) => props.theme.fontSizes.heading.small};

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    font-size: ${(props) => props.theme.fontSizes.heading.medium};
  }
`;

const Nav = styled.nav`
  display: none;

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    display: flex;
    gap: 1rem;
  }
`;

const NavLink = styled.a`
  color: white;
  text-decoration: none;
  font-size: ${(props) => props.theme.fontSizes.subHeading};
  &:hover {
    text-decoration: underline;
  }
`;

const HeroSection = styled.section`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 1rem;
  background-color: ${(props) => props.theme.colors.yellow};

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    flex-direction: row;
    justify-content: space-between;
    padding: 2rem;
  }
`;

const HeroContent = styled.div`
  text-align: center;

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    text-align: left;
    flex: 1;
  }
`;

const HeroTitle = styled.h2`
  font-family: ${(props) => props.theme.fonts.heading};
  color: ${(props) => props.theme.colors.red};
  font-size: ${(props) => props.theme.fontSizes.heading.medium};
  margin-bottom: 1rem;

  @media (min-width: ${(props) => props.theme.breakpoints.desktop}) {
    font-size: ${(props) => props.theme.fontSizes.heading.large};
  }
`;

const HeroImage = styled(Image)`
  border-radius: 50%;
  margin-top: 2rem;

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    margin-top: 0;
  }
`;

const Button = styled.button`
  background-color: ${(props) => props.theme.colors.red};
  color: white;
  border: none;
  padding: 0.5rem 1rem;
  border-radius: 5px;
  cursor: pointer;
  font-size: ${(props) => props.theme.fontSizes.subHeading};
  text-transform: uppercase;
`;

const ServicesSection = styled.section`
  padding: 2rem 1rem;
  background-color: ${(props) => props.theme.colors.blue};

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    padding: 2rem;
  }
`;

const ServicesList = styled.ul`
  list-style-type: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 1rem;

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    flex-direction: row;
    justify-content: space-around;
  }
`;

const ServiceItem = styled.li`
  text-align: center;
  font-size: ${(props) => props.theme.fontSizes.subHeading};
`;

const ButtonGroup = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1rem;
  margin-top: 2rem;

  @media (min-width: ${(props) => props.theme.breakpoints.tablet}) {
    flex-direction: row;
    justify-content: center;
  }
`;

// Main component
export default function EkloWebsite() {
  return (
    <ThemeProvider theme={theme}>
      <GlobalStyle />
      <Header>
        <Logo>eklo</Logo>
        <Nav>
          <NavLink href="#">About</NavLink>
          <NavLink href="#">Services</NavLink>
          <NavLink href="#">Contact</NavLink>
        </Nav>
      </Header>
      <HeroSection>
        <HeroContent>
          <HeroTitle>Workshop online</HeroTitle>
          <p>Thu 16.06.2021</p>
          <Button>Tham gia ngay</Button>
        </HeroContent>
        <HeroImage
          src="/placeholder.svg"
          alt="Workshop"
          width={300}
          height={300}
        />
      </HeroSection>
      <ServicesSection>
        <h2>Our Services</h2>
        <ServicesList>
          <ServiceItem>Workshop</ServiceItem>
          <ServiceItem>Coaching</ServiceItem>
          <ServiceItem>Public Speaking</ServiceItem>
          <ServiceItem>Soft Skills</ServiceItem>
        </ServicesList>
      </ServicesSection>
      <ButtonGroup>
        <Button>TÔI KỂ CHUYỆN</Button>
        <Button>PODCAST « TRÀ ĐÁ CÙNG EKLO »</Button>
        <Button>THƯ VIỆN ĐA PHƯƠNG TIỆN</Button>
        <Button>TIPS</Button>
      </ButtonGroup>
    </ThemeProvider>
  );
}
